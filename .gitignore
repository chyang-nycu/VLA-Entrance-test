# Python virtual environment (project-local, rebuildable from setup/)
.venv/

# Python bytecode caches
__pycache__/
*.pyc

# Generated videos and large datasets (task-local outputs, not source)
*.mp4
*.avi
*.mov
*.webm
datasets/
*.hdf5
*.npz

# Exception: small, decode-verified evidence videos explicitly committed as
# phase deliverables (same precedent as the committed evidence GIFs already
# under artifacts/) -- not a generated-debug-output dump.
!artifacts/phase4b_task1_nominal.mp4
!artifacts/phase4d_failure_reproduction.mp4
!artifacts/phase4e_task1_corrected.mp4
!artifacts/phase4e_task1_closeup.mp4
!artifacts/phase4f_task1_full.mp4
!artifacts/phase4f_bilateral_contact_view.mp4
!artifacts/phase5a_head_camera.mp4

# Exception: Phase 5B's 3-episode VLA demonstration dataset prototype is
# small (~1.7MB) and explicitly committed as a phase deliverable, not a
# generated-debug-output dump; regenerate via
# tasks/g1_pick_place/record_demonstrations.py (deterministic, see
# data/schema.md).
!data/task1_prototype.hdf5

# Exception: Phase 5C's two-rate (policy + execution) replacement/addition
# dataset is small (~5.0MB, mostly the added 500Hz execution trace) and
# explicitly committed as a phase deliverable; regenerate via
# tasks/g1_pick_place/record_demonstrations_v2.py (deterministic, see
# data/schema_v2.md). data/task1_prototype.hdf5 above is preserved
# unmodified as the original Phase 5B prototype evidence.
!data/task1_prototype_v2.hdf5

# Exception: Phase 5D's redesigned-policy-action dataset is small (~5.1MB)
# and explicitly committed as a phase deliverable; regenerate via
# tasks/g1_pick_place/record_demonstrations_v3.py (deterministic, see
# data/schema_v3.md). data/task1_prototype.hdf5 and
# data/task1_prototype_v2.hdf5 above are preserved unmodified as prior
# phases' evidence.
!data/task1_prototype_v3.hdf5

# Phase 5E's scaled-collection dataset (data/task1_demonstrations_v1.hdf5,
# ~62MB, 29 episodes) is deliberately NOT exempted here, unlike the small
# task1_prototype*.hdf5 files above -- it exceeds the phase's own 20MB
# commit-size threshold, so it stays covered by the *.hdf5 rule above.
# Checksum/size/regeneration command are recorded in
# reports/phase5e-scaled-data-collection.md and
# logs/phase5e_collection_summary.json instead; regenerate via
# tasks/g1_pick_place/collect_dataset.py (deterministic per
# data/task1_collection_spec.json's locked seed list).

# Temporary MuJoCo outputs
*.mjb
tmp_mujoco*/

# macOS
.DS_Store
