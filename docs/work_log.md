# Work Log

## 2026-09-02

- Inspected host: macOS 12.7.6, Intel `x86_64`, 16 GB RAM, 326 GiB free on workspace volume, Xcode Command Line Tools at `/Library/Developer/CommandLineTools`.
- Confirmed tools: Homebrew 6.0.21, CMake 4.4.3, Apple Git 2.37.1, Python 3.14.7, Python 3.12.14.
- Reviewed official `unitree_mujoco` README from GitHub and the cloned repository. It documents a Linux-first C++ simulator path and a Python simulator path using `unitree_sdk2_python`, `mujoco`, and `pygame`; G1 uses the `unitree_hg` message type.
- Cloned official repository into `vendor/unitree_mujoco`.
- Pinned `unitree_mujoco` commit: `4134cb5dc7ff1ba7f484deda48b5274b58694519` (`2026-08-25T22:03:03+08:00`, merge pull request #129).
- Created project-local Python virtual environment at `.venv` using Python 3.12.14.
- Attempted unpinned `pip install mujoco`; it selected `mujoco-3.12.0` source distribution and failed because `MUJOCO_PATH` was not set.
- Installed pinned Python simulator dependencies: `mujoco==3.3.6`, `numpy==2.5.2`, `imageio==2.37.4`, `pillow==12.3.0`, plus MuJoCo transitive dependencies.
- Ran `setup/g1_mujoco_smoke.py` against official `unitree_robots/g1/scene.xml`.
- Verified model load, simulation advance, reset, joint state read, and small safe actuator command changing simulated joint state.
- Attempted offscreen screenshot capture. It failed with `CGLError('invalid CoreGraphics connection')`, indicating no valid macOS graphics context in the terminal session.
- Noted macOS case-insensitive checkout warning for Go2w `terrain.STL` and `terrain.stl`; G1 smoke test was unaffected.

## 2026-09-02 Phase 2

- Inspected pinned G1 model at `4134cb5dc7ff1ba7f484deda48b5274b58694519`.
- Generated `logs/g1_actuators.json` with all 29 actuators, controlled joints, control ranges, joint ranges, and associated bodies.
- Audited G1 variants: `scene.xml`, `scene_23dof.xml`, `scene_29dof.xml`, `g1_23dof.xml`, and `g1_29dof.xml`.
- Confirmed no pinned G1 XML contains an actuated gripper, Inspire hand, or Dex3 hand. G1 hand/finger meshes exist as assets, but compiled variants do not expose actuated finger/gripper joints.
- Ran task-local contact experiment in `tasks/g1_pick_place/g1_contact_probe_scene.xml`; MuJoCo reported contact between `probe_cube_geom` and `right_wrist_yaw_link`.
- Ran free-standing stability probe; pelvis translated `0.9305553713743192` m in 2 seconds under small arm torques, supporting a fixed-base MVP.
- Added automated tests in `tests/test_phase2_g1_audit.py`; `unittest` passed 4 tests.

## 2026-09-02 Repository hygiene

- No `HANDOFF.md` existed anywhere in the workspace despite prior expectations that one did; regenerated it from actually-verified state plus the Phase 3 spec given directly by the project owner. Added top-level `README.md`.
- Added `.gitignore` (`.venv/`, `__pycache__/`, `*.pyc`, generated videos/datasets, temp MuJoCo outputs, `.DS_Store`).
- Registered `vendor/unitree_mujoco` as a git submodule (manual gitlink + `.gitmodules`, no re-clone) pinned at `4134cb5dc7ff1ba7f484deda48b5274b58694519`; existing checkout and its pre-existing `terrain.STL` case-collision artifact left untouched.
- Re-ran Phase 1 and Phase 2 tests (all pass) and made a local checkpoint commit (`dd2bc9b`), no push.

## 2026-09-02 Phase 3

- Built `tasks/g1_pick_place/gripper_scene.py`: task-local derived MJCF (ElementTree deep-copy of vendor `g1_29dof.xml`) adding a pelvis-fixing equality weld, a symmetric parallel-jaw gripper under `right_wrist_yaw_link` (two slide-joint fingers, motor actuators, collision pads, `grasp_tcp` site), a static table, and a free-jointed cube. Reachable workspace confirmed via real forward-kinematics sampling (wrist reaches `[0.231,-0.155,0.804]` at a representative arm pose).
- Built `tasks/g1_pick_place/controller.py`: DLS Cartesian IK on a scratch `MjData` (explicit joint/qpos/qvel/actuator index maps), bounded joint-space PD with gravity/Coriolis feedforward and finite-output enforcement. Verified IK converges to sub-mm/few-mm accuracy on realistic waypoints.
- Built `tasks/g1_pick_place/run_grasp_test.py`: pregrasp -> approach -> close -> lift -> hold -> lower -> open sequence, all 5 HANDOFF.md acceptance criteria checked directly against simulation telemetry; variant sweep gated on nominal success.
- Ran 3 documented tuning iterations on the nominal grasp trial (see `reports/phase3-grasping-baseline.md` for full detail): (1) baseline gains, no gravity comp — arm never reached the target, no contact; (2) added gravity/Coriolis feedforward + higher gains — contact achieved but no real grip/lift; (3) added finger overtravel/squeeze margin + trajectory-continuity fix — contact still one-sided, cube shoved out of position during approach before the gripper closes. Diagnosed root cause as multi-joint tracking oscillation from a single PD gain pair applied across joints with 5x differing torque limits; not fixed within the 3-iteration budget.
- Per `HANDOFF.md`'s tuning budget, stopped after iteration 3 rather than attempting a 4th or introducing a scripted grasp constraint. Nominal trial result: FAIL (height gain 0.005 m of required 0.08 m; 0.0 s of required 2.0 s continuous lift). 5-position-variant evaluation not run (gated on nominal success).
- Added `tests/test_phase3_gripper.py` (7 tests, pass), `tests/test_phase3_controller.py` (7 tests, pass), `tests/test_phase3_grasp.py` (6 tests, 3 fail honestly against the real acceptance criteria — not weakened). Wrote `logs/phase3_grasp_trials.json` and `reports/phase3-grasping-baseline.md`.

## 2026-09-02 Phase 3B (controller stabilization budget)

- Clarified and enforced the cube initialization boundary: `CubeInitGuard` in `run_grasp_test.py` allows cube qpos/qvel writes only before a trial's first `mj_step`, raises `RuntimeError` after `lock()`; `run_trial`'s own source is scanned at import time for any bypass. Added `tests/test_phase3_grasp.py::CubeInitGuardTest` (4 tests, pass). Refactored `run_trial` to accept an injectable `controller` and `diagnostics` dict without changing default behavior — reran the unmodified nominal trial and confirmed bit-identical output to Phase 3's recorded result (height gain 0.0051 m) before making any tuning changes.
- Instrumentation (required before Attempt 3B-1): confirmed via `model.actuator_gear`/`actuator_trntype` that all 7 right-arm motors are 1:1 joint-torque actuators (gear `[1,0,0,0,0,0]`, `mjTRN_JOINT`) — the `torque_limit_i/25` gain formula applies with no unit conversion. Gravity/Coriolis feedforward consumes under 10% of any joint's torque authority at rest — not the bottleneck. Baseline instrumented run: `wrist_roll` 92.4%, `wrist_pitch` 68.2%, `wrist_yaw` 74.8% torque-saturated; shoulder/elbow near 0%. TCP RMS error 0.061 m.
- Attempt 3B-1 (per-joint Kp/Kd scaled by torque authority, per HANDOFF.md formula): FAIL, and worse than baseline — fixed the targeted joints' saturation but the torque-agnostic DLS-IK reassigned kinematic load onto shoulder_yaw/elbow (saturation 5.6%→35.4%, TCP RMS error 0.061→0.215 m), losing cube contact entirely.
- Attempt 3B-2 (3B-1 gains + settle-before-close gating): FAIL, no better — the settle gate never converged within its 1.5 s budget, confirming this is a tracking-convergence problem, not a trajectory-speed problem.
- Attempt 3B-3 (reverted to baseline PD gains + torque-weighted DLS-IK, new optional `dls_weights` param on `solve_dls_ik`, `None` preserves original behavior): FAIL, closest of the three — regained cube contact and near-baseline TCP tracking, but wrist saturation only mildly reduced (1-5 points) and height gain still far short (0.0005 m of 0.08 m required).
- Root cause update: uniform PD gains and a torque-agnostic IK are two separate problems; fixing either alone does not fix the coupled system. A real fix likely needs both layers redesigned and re-tuned together — out of scope for one bounded adjustment.
- Per the Phase 3B budget, stopped after 3 failed attempts (did not attempt a 4th, did not relax acceptance criteria or add a scripted grasp constraint). Full quantitative analysis: `reports/phase3b-controller-stabilization.md`. All metrics: `logs/phase3b_controller_tuning.json`. Tests: `test_phase3_gripper.py` 7/7 pass, `test_phase3_controller.py` 7/7 pass, `test_phase3_grasp.py` 10 tests (4 boundary-guard pass, 3 supporting-criteria pass, 3 nominal-acceptance fail honestly, unchanged from Phase 3).

## 2026-09-02 Phase 3C (controller-architecture replacement) — PASS

- Authorized as an architecture replacement, not a 4th Phase 3B tuning attempt. Phase 3/3B reports, logs, and commits (`f5ce62d`, `dd29718`) are historical and were not edited; `tests/test_phase3_grasp.py`'s 3 nominal-acceptance failures still reproduce identically (reverified this session).
- Replaced the 7 right-arm `<motor>` torque actuators with bounded MuJoCo `<position>` servos (`write_grasp_scene_3c()` in `gripper_scene.py`), keeping each joint's real physical force limit (`forcerange`) and finite, validated gains. `write_grasp_scene()` (Phase 3/3B) verified byte-identical/unaffected via sha256.
- Two pre-attempt findings, each with direct evidence, before any tuning: (1) pelvis-only weld left the torso/waist unpowered and free to swing under arm reaction torque (TCP error grew unbounded, 0.06→0.50 m over 4.4 s) — fixed with a second `torso_link`-to-`pelvis` equality weld, task-local to the Phase 3C scene only. (2) Explicit-Euler integration (MuJoCo's default, since the vendor model has no `<option>` element) was numerically unstable with stiff position-servo gains regardless of gain choice — fixed by setting `integrator="implicitfast"`, no physical parameter changed.
- Redesigned IK in `controller_3c.py::solve_ik_waypoint`: position-priority (orientation left unconstrained — gripper is symmetric about its closing axis, and Phase 3/3B's failure was never orientation error), null-space joint-limit avoidance + nominal-posture objective, solved once per motion segment rather than every control step. Reachability diagnosis for PREGRASP/APPROACH/CLOSED_LIFT/HOLD run before any simulation: all 4 waypoints reachable, residuals 0.0027–0.0079 m. Evidence-based IK tolerance (8 mm) derived from a genuine wrist kinematic singularity found near the grasp waypoint (Jacobian singular value ≈5e-4), not chosen arbitrarily.
- New granular state machine (`RESET→PREGRASP→SETTLE_PREGRASP→APPROACH→SETTLE_APPROACH→CLOSE→VERIFY_BILATERAL_CONTACT→LIFT→HOLD→LOWER→OPEN→DONE/FAILED`) with settle-before-advance and bilateral-contact-before-lift gating, in `run_grasp_test_3c.py`. `CubeInitGuard` (Phase 3B's class) reused unchanged; a source-scan test confirms no direct cube-state write anywhere in `run_trial_3c` outside the guarded init.
- Attempt 3C-1 (first working implementation, historical gripper gains kp=40/kd=2): state machine reached DONE cleanly, bilateral contact achieved, but grip was too weak to hold through the LIFT transient — height gain 0.0497 m (need 0.08), hold 0.198 s (need 2.0), then dropped. FAIL (2/5 criteria).
- Attempt 3C-2 (gripper gain only, evidence-driven: friction back-of-envelope showed the 15 N force *limit* was not the bottleneck, the PD *gain* was; swept kp/kd, chose kp=150/kd=10 as the smallest change clearing the threshold with margin): height gain 0.1084 m, continuous hold 3.504 s. **PASS, all 5/5 criteria.** Attempts 3C-3/3C-4 not needed.
- Determinism: 5/5 identical reruns from the same seed (`height_gain_m` and `max_continuous_lifted_s` bit-identical across all 5).
- Regression tests reran this session: `test_phase3_gripper.py` 7/7 pass, `test_phase3_controller.py` 7/7 pass, `test_phase3_grasp.py` 10 total (3 fail, unchanged historical Phase 3 record), `test_phase3c_grasp.py` 18/18 pass (includes nominal acceptance + 5x determinism). Vendor pinned commit unchanged, verified.
- Full report: `reports/phase3c-position-servo-baseline.md`. All attempt metrics: `logs/phase3c_attempts.json`. Video: `artifacts/phase3c_grasp_demo.gif`. `HANDOFF.md` updated with the outcome; Phase 3/3B sections left unedited.
- Limitations carried forward: fixed-base is now pelvis+torso (more restrictive than originally framed); IK tolerance and gripper gains are specific to this cube's position/mass/friction; the 5-position-variant sweep from Phase 3's original spec has still not been run.

## 2026-09-02 Phase 4A (grasp setup-variant evaluation)

- Preserved baseline: reran `tests.test_phase3c_grasp.Phase3CNominalGraspTest` once, unmodified, at commit `bf57b74` — 8/8 pass. No IK/servo/gripper/state-machine parameter changed before starting variant work.
- Defined 5 deterministic variants per HANDOFF.md: nominal, +/-0.03 m on x, +/-0.03 m on y (table-plane axes), using the same 0.03 m default as HANDOFF.md's spec (table containment checked and trivially satisfied at this magnitude, no deviation needed). Built `tasks/g1_pick_place/run_variant_sweep.py`: one shared scene/config (`arm_kp=400, arm_kv=25, gripper_kp=150, gripper_kd=10`, unchanged from Phase 3C's 3C-2) for all variants, no per-variant tuning; cube pose set only inside `CubeInitGuard`'s pre-lock window, reused unmodified.
- Pre-run feasibility check per variant (table containment, PREGRASP/APPROACH IK residual, joint-limit margin, TCP/finger-pad closing-axis alignment vs nominal) computed and recorded for all 5 before any trial ran. `x_plus_0.03` (APPROACH residual 27.1 mm) and `y_minus_0.03` (8.43 mm) were flagged unreachable (>8 mm evidence-based tolerance) before simulation; both were still run and both failed identically at `SETTLE_APPROACH`, confirming the feasibility check was predictive rather than assumed.
- Result: **3/5 variants succeed (nominal, x-0.03, y+0.03), 2/5 fail (x+0.03, y-0.03) — 60% variant rate, 60% trial rate (9/15).** Target (>=3/5) met on the first, unmodified sweep; 0 of the 2 allowed global adjustments were used (none were needed, none manufactured). All 3 repeats per variant were bit-identical (deterministic sim, consistent with Phase 3C).
- Test-suite hygiene: converted `tests/test_phase3_grasp.py`'s 3 historical nominal-acceptance failures into regression diagnostics that assert the exact documented failure values persist (height gain 0.005125 m, hold 0.0 s, overall pass=False), with tight numeric tolerances so any accidental future change would be caught. No threshold lowered anywhere; no Phase 3C/4A test marked xfail; `reports/phase3-grasping-baseline.md`/`phase3b-controller-stabilization.md` unedited. Full suite: 58 passing current-controller tests + 3 passing legacy diagnostics + 0 unexpected failures.
- Added `tests/test_phase4a_grasp_variants.py` (12 tests, pass) asserting the feasibility-verdict/outcome consistency, shared-configuration invariant, nominal-reproduces-Phase-3C exactness, and the >=3/5 aggregate target.
- Limitations: the succeeding/failing envelope is asymmetric (-x/+y succeed, +x/-y fail), consistent with but not fully explained by the wrist singularity identified in Phase 3C (a full workspace Jacobian-conditioning map was not computed, out of scope). Full report: `reports/phase4a-grasp-variants.md`. Raw data: `logs/phase4a_grasp_variants.json`. `HANDOFF.md` updated with the outcome; earlier phase sections left unedited. Transport, target placement, cameras, dataset collection, and language variants remain unimplemented, per scope.

## 2026-09-02 Phase 4B (Task 1: complete pick-and-place)

- Added a static blue target pad (`_add_target_pad()` in `gripper_scene.py`, new `write_grasp_scene_4b()`) -- jointless geom, no equality/weld/tendon/actuator/force referencing it or the cube; `write_grasp_scene_3c()`'s output is unaffected (verified: no target geom in that scene).
- Target selection by direct IK-residual evidence over a grid of candidate offsets before any simulation: chosen `(-0.11, +0.07)` m offset (table pos `(0.22, -0.08)`), the candidate with the largest reachability margin (max residual 3.6 mm vs. 8 mm tolerance) among those also table-contained and requiring >=0.10 m of lateral transport. Rejected candidates and their residuals recorded in the report, including two that mirror Phase 4A's known-unreachable directions and one that fails table containment despite being IK-reachable.
- Extended the state machine in a new module (`tasks/g1_pick_place/run_pick_place.py`) -- does not modify `run_grasp_test_3c.py`/`controller_3c.py`/`controller.py`/`run_grasp_test.py`; RESET..HOLD reuses Phase 3C's constants unchanged (imported, not retyped) so grasp physics are byte-for-byte identical. New states: TRANSPORT_ABOVE_TARGET, SETTLE_ABOVE_TARGET, LOWER_TO_TARGET, SETTLE_LOWER, VERIFY_RELEASE, RETREAT, VERIFY_TASK_SUCCESS.
- Stage A tuning (nominal only, transport/lower trajectory parameters only, no gripper/arm/grasp-approach changes): **Attempt 1** (single-shot IK solve per segment, Phase 3C's style) failed at TRANSPORT_ABOVE_TARGET -- a one-shot position-servo step across the ~0.13 m lateral move caused a hard acceleration transient that broke bilateral contact (tracking error peaked at 0.54 rad). **Attempt 2** (20/8 linearly-interpolated Cartesian sub-waypoints, each driven as a fixed set-point) fixed the transport failure but LOWER_TO_TARGET still intermittently lost contact in its final ~15%; ruled out release-height clearance and null-space posture bias as causes; found the real cause was each sub-waypoint's *stepped* (not ramped) position-servo target, whose repeated small jerks accumulated slip in the closed grip. **Attempt 3** (commanded joint target ramped linearly between consecutive sub-waypoint solutions instead of stepped; 40/2.0s transport, 60/2.0s lower) **PASSED**, 0 lost-contact steps, deterministic 5/5.
- Also found and fixed, before Attempt 1 was scored: an implementation bug (not a tuning attempt) where the transport-abort height check was wrongly applied to LIFT itself (where starting below the lift threshold is expected, not a violation).
- Also found and fixed during Attempt 3 investigation: the cube-slip diagnostic was computed as a raw world-frame vector difference, which conflated genuine slip with the wrist's own ~13-15 deg rotation during the large transport move. Fixed to measure slip in the TCP's own rotating local frame.
- Objective task-success detector (8 conditions, all required continuously for a 0.5 s dwell, streak resets on any single-step violation -- never counts a first boundary crossing): cube lifted >=0.08 m; released by both pads; cube center within the pad's 15 mm XY margin; supported by the pad, not the gripper; linear/angular speed below threshold; all continuously true for the dwell; robot retreated (<5 mm cube disturbance) without disturbing the cube.
- Stage A nominal result: `task_pass=True`, deterministic 5/5, height gain 0.10839 m (identical to Phase 3C/4A), final XY target error 14.6 mm (against a 15 mm margin -- tight but a reliable, deterministic pass, no randomness in this pipeline), max measured cube slip (local-frame, corrected) 0.156 m -- a genuine, honestly-reported tradeoff: slower/finer motion eliminates hard contact-breaking transients but allows more gradual compliant repositioning under the longer sustained load; contact itself was never lost.
- Stage B (same fixed target, Phase 4A's 3 reachable variants, 3 trials each, one shared config, no per-variant tuning): nominal and x-0.03 complete the full task (3/3 each); y+0.03 grasps successfully (3/3 grasp_pass, contact never lost) but misses the placement margin (20.4 mm > 15 mm) in all 3 trials -- a genuine placement-accuracy limit, not a dropped grasp. **Supported-envelope success 2/3 (67%); original five-variant coverage 2/5** (x+0.03/y-0.03 remain excluded, unchanged from Phase 4A, listed separately).
- Test-suite hygiene unchanged: the same 3 Phase 3 legacy diagnostics remain untouched. Added `tests/test_phase4b_pick_place.py` (36 tests, pass) covering target geometry/no-cube-constraints, reachability, state-machine ordering, a genuine (unmocked) loss-of-grasp abort, release detection, success-detector dwell/boundary behavior, the nominal result's exact numbers, determinism, the 3 Stage-B variants, the initialization-boundary self-audit, and vendor integrity. Full suite this session: 94 tests, 0 unexpected failures.
- No `.mp4`/GIF demo produced: no `ffmpeg`/`imageio-ffmpeg` available in the environment and installing a new dependency was not authorized -- noted honestly rather than skipped silently.
- Full report: `reports/phase4b-task1-pick-place.md` (rejected/chosen target candidates, full attempt log, both Stage B tables, limitations). Raw data: `logs/phase4b_pick_place_trials.json`. `HANDOFF.md` updated with the outcome; earlier phase sections left unedited. Cameras, dataset collection, language-conditioned variants, and policy integration (Task 2) remain unimplemented, per scope.

## 2026-09-02 Phase 4C (Task 1 evidence hardening and video capture)

- Audited Phase 4B's `max_cube_slip_m` (0.1561555402975266, nominal). Root cause found and confirmed (not guessed): the code kept accumulating "slip" on every step for the rest of the trial once a grasp reference was captured at `VERIFY_BILATERAL_CONTACT`, with no check that the cube was still being carried -- so `OPEN`/`RELEASE_SETTLE`/`VERIFY_RELEASE`/`RETREAT`/`VERIFY_TASK_SUCCESS` (all post-release) were silently included. Confirmed directly: `post_release_tcp_cube_separation_m = 0.14860671167299208` for the nominal trial, within 2 cm of the old figure. The TCP-local-frame transform itself (`R_tcp^T @ (cube_pos - tcp_pos)`) was already mathematically correct -- rotation handling verified via a synthetic unit test (pure TCP rotation with a rigidly-attached cube -> exactly zero slip); the defect was entirely the time window, not the math.
- Implemented corrected metrics in `tasks/g1_pick_place/run_pick_place.py`, gated on the existing `carrying` signal (already used elsewhere in the same function) so slip only accumulates while the gripper is closed and the cube is grasped: `grasp_reference_offset_tcp_frame`, `max_slip_during_lift`, `max_slip_during_transport`, `max_slip_during_lower`, `slip_at_release`, `post_release_tcp_cube_separation_m` (never called slip). Legacy `max_cube_slip_m` retained unmodified for reproducibility of the historical figure. Slip-frame math factored into two pure functions (`tcp_local_cube_offset`, `relative_slip_m`) for unit-testability; refactor verified behavior-preserving by rerunning the nominal trial before/after and observing byte-identical `height_gain_m`/`final_xy_target_error_m`/`max_cube_slip_m`.
- **Corrected genuine grasp-phase slip: 3.3-5.4 cm** (nominal: lift 0.032 m, transport 0.045 m, lower 0.052 m, release 0.020 m), not the old 14.97-15.62 cm. No pass/fail outcome changed anywhere -- slip was never an acceptance criterion. No controller/gain/trajectory parameter changed (verified via diff against `363aa83`). `reports/phase4b-task1-pick-place.md` left unedited; correction lives in the new `reports/phase4c-task1-evidence.md` as a labeled addendum.
- Added `tests/test_phase4c_slip_audit.py` (10 tests, pass): 3 synthetic math unit tests (pure world translation -> 0 slip, pure TCP rotation with rigid cube offset -> 0 slip at 4 angles, known 5mm relative displacement -> reports 5mm exactly), 6 tests confirming post-release isolation on the real nominal trial (task-pass/height/XY-error unchanged, legacy field reproduces the historical figure, post-release separation explains the old number, all corrected grasp-phase metrics far below the old number and the cube's footprint, `slip_at_release` unaffected by later retreat), 1 test confirming the fix holds on the y+0.03 variant's different (placement-failure) code path.
- Video capture: installed `imageio-ffmpeg` into the existing project venv (user-local, no admin install) -- worked on first attempt, no GIF fallback needed. Added a purely additive, opt-in `frame_callback` parameter to `run_trial_pick_place()` (default `None`, zero effect on any existing caller -- confirmed via full suite rerun) to capture frames without touching control/physics. Rendered `artifacts/phase4b_task1_nominal.mp4` via a single fixed (non-tracking) third-person `MjvCamera`: 640x480, 29.41 fps (real-time-synchronized physics stride), 375 frames, 12.75s, 229,060 bytes, decode-verified by reading the file back and matching frame count/fps. Also captured 3 still frames (grasp, mid-transport, released-in-target).
- Test-suite hygiene unchanged: same 3 Phase 3 legacy diagnostics untouched. Full suite this session: 104 tests, 0 unexpected failures (94 pre-existing + 10 new).
- Full report: `reports/phase4c-task1-evidence.md` (old-vs-corrected slip definitions, per-variant table, video/still details, full test/verification summary). Raw audit data: `logs/phase4c_slip_audit.json` (produced by `tasks/g1_pick_place/audit_slip_metric.py`). `HANDOFF.md` updated with the outcome; earlier phase sections left unedited. Task 2 (cameras as a data-collection feature, dataset pipeline, language-conditioned variants, policy integration) remains unimplemented, per scope.

## 2026-09-02 Phase 4D (physics-integrity investigation -- diagnosis only, no fix)

- Triggered by a user visual GUI inspection of the committed Task 1 pipeline (commit `dfeec9e`) reporting two apparent defects: (1) hand/fingers visibly pass through the cube, (2) the cube visibly falls instead of being lifted. Investigated skeptically from scratch, not by defending the existing 104-test suite.
- **Defect (1) CONFIRMED and reproduced.** Traced the vendor G1 model's own `right_rubber_hand` geom under `right_wrist_yaw_link` in `g1_29dof.xml`: a static, non-articulated visual mesh, explicitly `contype="0" conaffinity="0"` in the vendor's own authoring (i.e. collision-free by design, not something this project disabled). Computed its bounding box directly from the STL file (`[0.0415, 0.1733]` m along the wrist's local x-axis after applying the vendor's own geom offset) and found it fully overlaps this project's real functional finger pads (`[0.088, 0.112]` m, same local axis). Confirmed visually with a zoomed render at CLOSE and HOLD (`artifacts/phase4d_collision_debug_close.png`, `_hold.png`): the large white decorative hand mesh clips straight through the cube while the actual (much smaller, dark) finger pads grip it correctly off to the side.
- **Defect (2) NOT reproduced.** Isolated cube/table settling test (real Task 1 scene, no robot motion, 3s pure gravity): cube settles within 0.22mm of the table top, vertical velocity decays to ~2e-14 m/s, per-step summed contact force balances the cube's weight to 4 significant figures (0.4906 N measured vs. 0.4905 N true weight), never approaches passing through the table. Fresh instrumented rerun of the real nominal trial: genuine 0.108m height gain, real nonzero bilateral contact force during HOLD (min 0.234 N, not a zero-force "contact detected" false positive). Most likely explanation for the visual report: the same decorative-mesh confusion in (1) makes the real grip look precarious/asymmetric (the functional pads visibly touch only a lower/side region of the cube in the CLOSE render, not a centered squeeze that "looks" secure), or an intended `LOWER_TO_TARGET`/`OPEN` transition was misread as a drop -- not treated as a second confirmed bug.
- Classified all 76 tests across `test_phase3c_grasp.py`/`test_phase4a_grasp_variants.py`/`test_phase4b_pick_place.py`/`test_phase4c_slip_audit.py` by what they actually check: 50 real end-to-end (fresh-simulated every run, confirmed zero reliance on cached `logs/*.json` results via `grep`), 18 structural, 5 kinematic/reachability, 3 synthetic math. Zero mocked, zero log-regression (the only log-regression tests in the repo are the 3 unrelated historical Phase 3 diagnostics in a different file). Conclusion: the suite passed honestly on everything it actually checked -- it simply never had any coverage for "does an unrelated, collision-free visual geom spatially overlap the cube," which is a pure rendering property distinct from every contact/height/velocity check the suite does perform.
- Added `tests/test_phase4d_physics_integrity.py` (13 tests): 12 pass (cube-identity tracking, isolated table-support physics, a fresh genuine-lift/nonzero-force re-verification), 1 **fails on purpose** (`Phase4DDecorativeHandOverlapTest`) to keep the confirmed, unfixed decorative-mesh overlap visible in the default test run rather than hiding it. Full suite after this phase: 117 tests, 1 expected failure -- not a regression, not something to quietly patch by loosening the test.
- Diagnostic-only harness `tasks/g1_pick_place/phase4d_reproduce_defect.py` written to reproduce/log everything above without touching any production/controller/scene file; raw findings in `logs/phase4d_physics_integrity.json`. Rendered `artifacts/phase4d_failure_reproduction.mp4` (zoomed on the gripper through CLOSE/LIFT/HOLD) plus the two debug stills.
- **No fix implemented.** No historical report/log/commit modified or reinterpreted. Recommendation for a future authorized phase: hide/remove the vendor's decorative hand meshes in the task-local scene generator (same copy-and-edit-the-deep-copy pattern already used for every other task-local scene change) -- carries no physics risk since `contype`/`conaffinity` are already 0.
- Full report: `reports/phase4d-physics-integrity-audit.md` (root-cause writeup, all 9 regression gates scored with evidence, full test classification). `HANDOFF.md`/`README.md` updated with the required visible notice ("Previous Task 1 success results are under review following visual detection of collision/support inconsistencies."). No dataset collection, cameras-as-a-feature, or Task 2 work started.

## 2026-09-02 Phase 4E (gripper visual/collision repair and grasp-stability redesign)

- Authorized after a user frame-by-frame review of `artifacts/phase4d_failure_reproduction.mp4` found: (1) cube does rise in world Z; (2) it visibly slides down relative to the gripper throughout HOLD (near-drop); (3) the decorative hand still penetrates the cube; (4) the collision gripper is visually inconsistent with the displayed hand. Noted for the record: the authorization's quoted `slip_at_release` (~0.0519m) was a mix-up with `max_slip_during_lower`; the actual `slip_at_release` in `logs/phase4c_slip_audit.json` is 0.0198m -- does not change the underlying finding (real grasp-phase slip on the order of 1-5cm on a 7cm cube).
- **Section A (visual fix):** removed the vendor `right_rubber_hand` geom, added a palm backing plate, and gave the two finger pads distinguishable colors -- scoped ONLY to `write_grasp_scene_4b` via two new `_build_grasp_tree` parameters (`finger_pad_half`, `apply_phase4e_gripper_visuals`), both defaulting to Phase 3/3B/3C's original behavior. Verified zero ripple into Phase 3/3B (`write_grasp_scene()`'s sha256 unchanged: `1b2fd577...`) and Phase 3C/4A (`run_trial_3c`'s nominal height gain unchanged: `0.10838913826086727`).
- **Section B (evidence):** wrote `tasks/g1_pick_place/phase4e_diagnose_grip.py` to measure, from the model itself (not assumed): cube mass 0.05kg, gravity 9.81, cube friction 1.0, pad friction 1.2, effective mu 1.2 (MuJoCo's default max-combination rule), N_min = m*g/(2*mu) = 0.2044N. Baseline safety factors: CLOSE 0.328x, **LIFT 0.146x (below 1x -- physically-caused slip)**, HOLD 1.068x (razor-thin). Contact Z-offset from cube center ranged -4.16cm to +5.05cm (mean +1.53cm), confirming intermittent edge/corner contact.
- Root cause 1 (force): LIFT still used Stage-A-Attempt-1's one-shot `_drive_segment` (never updated when the same pattern was fixed for TRANSPORT/LOWER in Phase 4B, since LIFT itself wasn't implicated at the time). Root cause 2 (geometry): `solve_ik_waypoint` has no orientation term, so wrist roll drifts freely, and because the fingers are offset from the TCP site in local Y, that drift shows up as real world-Z misalignment between the pads and the cube center.
- **3 repair attempts** (visual+pad-height increase 0.022->0.030m; LIFT smoothed via `_drive_smooth` 20wp/1.2s + gripper gains 150/10->260/16; LIFT 30wp/1.5s + gains ->320/20). A real telemetry bug was caught and fixed mid-Attempt-2: smoothed LIFT's per-waypoint phase strings (`"LIFT_wp3"`) didn't match the exact-string slip-bucketing check, silently dropping LIFT-phase slip samples -- fixed (`phase == "HOLD" or phase.startswith("LIFT")`) before trusting any number. Final result: max slip while grasped fell from as much as 5.19cm (baseline) to 2.05cm (Attempt 3); worst-instant safety factor rose from 0.054x to consistently >=1.0x (CLOSE 1.025x, HOLD 2.232x). **Still ~2x over the new <=10mm bound.** No 4th attempt, no threshold loosened.
- **Section C:** implemented `criteria_grasp_stability_4e`/`grasp_stability_pass_4e` on `run_trial_pick_place`'s result. Result: 3/5 sub-criteria pass (downward-slip-during-hold 1.87mm<=5mm; bilateral-contact-throughout-hold; [max-slip-while-grasped and cube-center-vertical-overlap and normal-forces-positive-and-finite all FAIL]) -- deterministic across 5 reruns. A genuinely new finding: at least one exactly-zero-force contact instant occurs during the grasped window even though the boolean geometric-contact check never reports a loss -- exactly the "contact reported != nonzero force" failure mode Phase 4D's directive warned about.
- **Section D:** required gate (max slip <=10mm) not met, so Stage B was explicitly NOT run as an authorized evaluation -- ran informationally only (all 3 variants now complete the full task under the old criteria, a genuine incidental improvement over Phase 4B's 2/3, recorded in `logs/phase4e_gripper_integrity.json` under `stage_b_informational_only_gate_not_met`, not to be cited as a Section D result).
- **Section E:** rendered `artifacts/phase4e_task1_corrected.mp4` (640x480, 29.41fps, 389 frames, 13.23s, decode-verified) and `artifacts/phase4e_task1_closeup.mp4` (480x368, same fps/frames/duration, decode-verified), both with burned-in overlay (task state, cube height gain, live TCP-frame slip, L/R contact force) via `tasks/g1_pick_place/record_corrected_episode.py`. 3 new still frames. Neither `phase4d_failure_reproduction.mp4` nor `phase4b_task1_nominal.mp4` was touched.
- **Test-suite hygiene:** `tests/test_phase4d_physics_integrity.py`'s previously-failing-on-purpose test now genuinely passes (rewritten to check the real generated scene, not a permanently-true static vendor-STL fact -- old static check kept as a separate context-only test). `tests/test_phase4b_pick_place.py`/`test_phase4c_slip_audit.py` frozen-value assertions updated to the new, legitimately-changed numbers (documented inline, old value + reason); these test the *active* pipeline, unlike Phase 3's frozen historical controller. One test's adversarial stress parameters strengthened (the stronger grip survives the old stress test); one boundary-case test's failing example was replaced (`y_plus_0.03` now passes -- a genuine improvement) with a pre-declared-unreachable variant, flagged as a weaker substitute. Added `tests/test_phase4e_gripper_integrity.py` (21 tests, all genuinely passing, including tests that assert the CURRENT partial-failure state honestly rather than a hoped-for pass).
- Full report: `reports/phase4e-gripper-integrity-repair.md` (before/after tables, full attempt log, N_min derivation, Section C criteria table, Section F regression check, honest limitations). Raw data: `logs/phase4e_gripper_integrity.json`. `HANDOFF.md`/`README.md` updated. **Task 1 success is NOT restored** -- pending human video review and a future phase closing the remaining slip gap. No Task 2/cameras/dataset-collection work started.

## 2026-09-02 Phase 4F (orientation-constrained grasp stabilization)

- Authorized after human review of `phase4e_task1_closeup.mp4` did not approve Task 1: decorative-hand fix confirmed good, cube genuinely lifts/places, but the cube still slides ~20.5mm downward relative to the gripper (above the 10mm bar), bilateral finger placement wasn't visually clear, and the grasp looked offset below/away from gripper center. Primary hypothesis: `solve_ik_waypoint` (Phase 3C) is position-only, so wrist orientation drifts freely during descent.
- **Important process correction mid-phase**: initially wired the new oriented-IK/pad-mount-fix behavior directly into the SHARED `write_grasp_scene_4b()`/`run_trial_pick_place()` used by Phase 4B/4C/4D/4E -- this silently broke 19 previously-passing tests across those phases' own test files (real regressions, not flakiness: `test_nominal_task_pass`, `test_deterministic_across_five_reruns`, several Phase 4C slip-audit tests, etc., since the actual simulated trajectory changed). Caught by running the full suite before committing anything. Fixed by making Phase 4F's changes strictly additive/opt-in: new `write_grasp_scene_4f()` (in `gripper_scene.py`, mirrors `write_grasp_scene_4b()` plus the pad-mount fix) and a new `use_oriented_ik: bool = False` parameter on `run_trial_pick_place()` (and a `_solve_waypoint()` dispatch helper) -- every existing caller's default behavior is now confirmed byte/physics-identical to before Phase 4F (full pre-existing 139-test suite passes unmodified).
- **Section A**: `solve_ik_waypoint_oriented()` (new, `controller_3c.py`) -- null-space orientation objective (same priority tier as existing joint-limit/posture terms, so position accuracy is never traded away), required axis = wrist local Z (the finger pad's "tall" axis) aligned to world vertical, `ORIENT_TOL_RAD = arcsin(0.005/FINGER_CONTACT_Y) ≈ 7.0 deg` (evidence-derived, same style as `IK_POS_TOL`).
- **Key mid-phase finding**: printed the actual wrist rotation matrix at the real, converged nominal APPROACH configuration -- local Y (jaw axis) was ALREADY well-aligned to world Y (~4-5 deg off; left/right finger symmetry was never the problem), but local Z was ~47 deg off vertical (the arm reaches down at a steep diagonal, not horizontally). A pad box tilted 47 deg contacts the cube's vertical face at a CORNER, not flush -- explains the reported near-drop far better than a simple height mismatch (whose own contribution is only ~3mm from the Y-axis analysis).
- **Attempt 1** (orientation objective, `orient_weight=0.6`): real full-trial rerun -- orientation residual at APPROACH improved only marginally (47.4 -> 44.5 deg); slip unchanged (0.0219m vs. Phase 4E's 0.0205m baseline).
- **Attempt 2** (increased `orient_weight` to 2.0, plus a separate co-primary-stacked-DLS diagnostic sweep): found a genuine kinematic reachability conflict, not a tuning gap -- reaching the 7 deg orientation tolerance at this exact Cartesian point requires 30-70mm of TCP position error (missing the cube entirely). Real full trial at weight 2.0 FAILED even earlier, at SETTLE_APPROACH (no grasp attempted at all). Reverted to `orient_weight=0.6`.
- **Attempt 3** (measured finger-pad mounting correction, `FINGER_MOUNT_FIX_QUAT` in `gripper_scene.py`): computed the exact fixed rotation (about the wrist's own local Y/jaw axis) that levels local Z to ~4 deg from vertical at the real nominal APPROACH configuration, applied to each finger BODY's own frame only (its `pos` origin stays bit-identical -- verified by a dedicated test) so the joint's slide axis and the finger's bracketing of the TCP/cube target are unaffected. Real result: contact-z-offset metric improved ~17% (43.8mm -> 36.5mm) but overall slip did NOT improve (rose slightly to 25.9mm) -- slip during this grasp is evidently also driven by dynamic effects (impact/settling/friction transients during CLOSE/LIFT), not solely by static contact-patch geometry.
- No 4th attempt made; no threshold loosened. Final configuration (Attempt 3's) carried forward since it's the only attempt that improved a real physical measurement without regressing further than Attempt 1.
- **Section C**: 7 of 11 tightened acceptance criteria pass; the grasp-quality-critical ones (max 3D slip <=10mm: 25.9mm measured; cube-center-within-pad-vertical-overlap: 36.5mm > 30mm bound; physical release/settled placement: trial fails before OPEN) fail. Deterministic across 5 reruns.
- **Section D (Stage B)**: gate not met, so run informationally only, per HANDOFF.md's own requirement -- all 3 Phase-4A-reachable variants fail the same gate (SETTLE_LOWER) at comparable slip magnitudes.
- **Section E**: rendered `artifacts/phase4f_task1_full.mp4` (640x480, 29.41fps, 352 frames, 11.97s, decode-verified) and `artifacts/phase4f_bilateral_contact_view.mp4` (480x368, same fps/frames/duration, decode-verified) via `tasks/g1_pick_place/record_orientation_episode.py`, with a live overlay of state/position-residual/orientation-residual/total-slip/vertical-slip/contact-force (legibility confirmed by extracting and reading sample frames). The diagnostic camera's azimuth (155 deg) was chosen from an 8-way sweep of rendered test frames specifically so neither finger pad is fully hidden behind the cube -- confirmed visually, not by angle math alone.
- Added `tests/test_phase4f_orientation_grasp.py` (20 tests, all genuinely passing, several honestly asserting the current partial-failure numbers rather than a hoped-for pass -- e.g. pinning the exact 0.02592m slip value with an explicit "replace, don't loosen" instruction for any future fix). Full regression: 159 tests, 0 unexpected failures (139 pre-existing unchanged + 20 new).
- Full report: `reports/phase4f-orientation-grasp-stabilization.md` (orientation-IK design/justification, full per-attempt evidence table, 11-criteria pass/fail table, Stage B table, honest limitations). Raw data: `logs/phase4f_orientation_grasp.json`. `HANDOFF.md`/`README.md` updated. **Task 1 success is NOT restored** -- pending human review of the two new videos and a further, separately-authorized phase addressing the documented kinematic reachability conflict. No Task 2/cameras/dataset-collection work started.

## Phase 4F human acceptance decision (2026-09-02)

- Human reviewed `artifacts/phase4f_task1_full.mp4` and `artifacts/phase4f_bilateral_contact_view.mp4`. Decision: visually/functionally acceptable for the entrance-test prototype; decorative-hand fix confirmed holding; **Task 1 restored as "prototype task completed with a documented grasp-slip limitation."** Strict <=10mm engineering-quality slip bar explicitly still FAILS (measured 0.02592m, unchanged) and is not claimed to pass -- this is an acceptance-policy decision, not a data change.
- No controller retuning performed this phase (none authorized). No log, threshold, test assertion, video, or historical report edited.
- While cross-checking the acceptance criteria for the decision record, found an arithmetic slip in `reports/phase4f-orientation-grasp-stabilization.md`'s own summary line ("7 of 11 pass"): its own criteria table names exactly 3 failures (max-slip, pad-vertical-overlap, release/placement), so 11-3=8 should pass, not 7 -- confirmed against `logs/phase4f_orientation_grasp.json`'s `criteria_grasp_stability_4f` dict (8 true / 3 false). Historical report left unedited per instruction; the correct count (8/11) is used going forward in `reports/phase4f-human-acceptance-decision.md`, `README.md`, and `HANDOFF.md`.
- Created `reports/phase4f-human-acceptance-decision.md` (decision record: date, videos reviewed, what improved, measured slip, why accepted despite missing the strict bar, limitations/future work, explicit no-test-weakened statement). Updated `README.md` (top capability statement + notice + Phase 4F entry + new decision-entry + test-command annotation) and `HANDOFF.md` (superseded-notice annotation on the original under-review notice + new decision section appended at the end, original left in place).
- Full suite rerun unmodified: 159 tests, 0 unexpected failures (same count as end of Phase 4F -- no test was added, removed, or changed by this phase). The 2 diagnostic tests documenting the strict-bar failure (`test_max_slip_while_grasped_still_exceeds_tightened_bar`, `test_grasp_stability_pass_4f_is_honestly_false`) continue to pass by asserting the failure persists, per the same pattern already used for Phase 3's historical failures.

## Phase 5A -- onboard RGB observation smoke test (2026-09-03)

- Inspected the vendor G1 model for an existing head/camera-mount body before adding anything: found none -- `head_link` is a static geom on `torso_link`, no separate head body or joint. Attached the new `head_cam` `<camera>` element as a child of `torso_link`.
- **Bug caught during camera-pose iteration**: first attempt wrote a world-designed position directly into the MJCF `pos` attribute without converting to the parent body's local frame (a `<camera>` child's `pos` is local to its parent, not world) -- placed the camera ~0.85m too high, producing a bird's-eye view of the whole scene rather than a head-level shot. Fixed by explicit `cam_pos_local = cam_pos_world - torso_link_world_pos` (valid since `torso_link` has identity orientation at reset).
- Second iteration then sat *inside* the head mesh's own bounding volume (computed from the actual STL: local z in [0.325,0.530], offset into world z in [1.118,1.323]), causing visible self-occlusion by the robot's own head geometry. Final pose moved forward of the mesh's own front-face x-extent (world x=0.11 vs. mesh x_max≈0.074).
- Verified visibility across a **full fresh nominal Task 1 episode**, not just the reset frame, at all 10 named phases plus a first-bilateral-contact event: red cube visible at all 11 checkpoints; blue target visible at all 8 required pre-placement checkpoints (target pixel count drops sharply at OPEN/VERIFY_TASK_SUCCESS -- confirmed this is the cube physically covering the pad after a real successful placement, not a detection failure).
- **Configuration decision, documented rather than silently substituted**: Phase 4F's orientation-IK config (`use_oriented_ik=True`) does not reach OPEN/VERIFY_TASK_SUCCESS in its own logged behavior (fails at SETTLE_LOWER) -- confirmed by re-running it with this phase's camera attached, identical outcome. Since onboard evidence at those two states is required, used Phase 4B/4C's originally-completing configuration instead (same cube/target/gripper/arm-gain constants, only Phase 4F's own unsuccessful orientation objective differs). No retuning of either configuration.
- **Two bugs caught in my own smoke-test diagnostics, before trusting their output**: (1) the "camera pose constant" check used an exact-equality tolerance (`atol=1e-9`) and initially failed -- investigated rather than loosened blindly, found the pelvis+torso weld is a soft/finite-stiffness constraint, not bit-exact rigid; measured max deviation is 0.1876mm over the whole 13.24s episode, two-plus orders of magnitude below any task-relevant dimension, so the tolerance was corrected to a physically-meaningful 1mm bound instead of loosened arbitrarily. (2) the "no two consecutive frames identical" check was comparing semantically-unrelated named milestone frames (e.g. RESET vs PREGRASP, captured at very different sim instants) instead of temporally-adjacent video frames, plus had an accidental duplicate-frame bug (reset frame counted twice) -- fixed by measuring on the actual FRAME_STRIDE-sampled video sequence (389 genuinely-adjacent pairs, 0 identical).
- Confirmed rendering is read-only w.r.t. physics: reran the same trial with and without the camera/render calls active, `height_gain_m`/`task_pass`/`final_xy_target_error_m` bit-identical.
- Measured performance directly: sim-only 4.970s wall (2.66x realtime) vs. sim+render 20.985s wall (0.63x realtime) for a 13.244s episode; achieved combined throughput 18.54 fps. Recommended future policy observation rate: **10 Hz** (~2x headroom under the measured ceiling; 20 Hz noted as viable only for offline/non-real-time dataset generation).
- Created `tasks/g1_pick_place/camera_observation.py` (pose/intrinsics/extrinsics helpers, `write_grasp_scene_5a()`, smoke-test-only color masks calibrated against actually-sampled scene colors incl. MuJoCo's own sky-blue, to avoid a naive blue-mask false positive), `tasks/g1_pick_place/record_onboard_camera_episode.py` (runner), `tests/test_phase5a_onboard_camera.py` (19 tests: synthetic pose/intrinsics math, scene-isolation checks, and a real fresh-simulation trial), `logs/phase5a_camera_smoke.json`, `reports/phase5a-onboard-camera.md`, `artifacts/phase5a_head_camera.mp4` (onboard view only, 160x120, 29.41fps, 390 frames, 13.26s, decode-verified) and `artifacts/phase5a_head_camera_frames/` (11 PNGs). Added a `.gitignore` exception for the new mp4 (same blanket-`*.mp4`-rule pattern as Phase 4C/4E/4F's videos). Deleted a stray iteration scratch file (`g1_grasp_scene_5a_iter.xml`) before committing -- not a deliverable.
- Full suite: 178 tests, 0 unexpected failures (159 pre-existing unchanged + 19 new). Confirmed zero diff on `controller.py`, `controller_3c.py`, `run_pick_place.py`, `gripper_scene.py`, every existing scene XML, and every historical report/artifact against commit `c9352ac`. Vendor pin unchanged; vendor XML confirmed to not contain "head_cam" anywhere. No Task 1 controller/gain/threshold change. No Task 2/HDF5/training work started.

## Phase 5B -- VLA demonstration dataset prototype (2026-09-03)

- Authored `data/task1_canonical_config.json` FIRST, before any collector/validator/replay code, per the phase spec -- every field (scene generator, controller path, `use_oriented_ik=false`, arm/gripper gains, success thresholds, camera spec) read directly from the live code, not asserted from memory. Camera parent body (`torso_link`) reconfirmed by a fresh `ElementTree` parse of the generated scene. `tasks/g1_pick_place/canonical_config.py` computes/stamps/verifies a SHA-256 content hash of the manifest; the collector, validator, and replay tool all import it and fail loudly (`ManifestMismatchError`) on any live mismatch.
- **Resolved the outstanding Phase 5A resolution discrepancy**: checked the true `mujoco.Renderer` output array shape directly (`(120, 160, 3)`), confirming 160x120 is the real observation resolution and the Phase 5A video's 160x128 (ffprobe) is H.264 encoder macroblock padding, not a genuine spec mismatch. Documented in the manifest's `camera.resolution_note`.
- Ran the required pre-collection canonical-baseline trial (`tasks/g1_pick_place/phase5b_baseline_check.py`, evidence in `logs/phase5b_baseline_check.json`): all 5 checks (corrected gripper present, decorative hand absent, Task 1 physically completes, RGB renders non-blank, live hash matches manifest) passed on the first run.
- **Discovered mid-phase, before collecting episode 3**: the user's authorized plan named `y_plus_0.03` as the labeled placement-failure episode, based on Phase 4B/4C history. Re-ran it under the CURRENT canonical config (which includes Phase 4E's gripper-gain increase to 320/20 and LIFT/TRANSPORT/LOWER trajectory smoothing, both added after Phase 4B) and found it now passes deterministically (`task_pass=True`, xy_err=2.07mm) -- those later improvements closed the placement-margin gap that variant used to expose in Phase 4B. Swept a range of nearby/alternate offsets (y in [0.026,0.05], x in [-0.039,0.045], several diagonals) and found no remaining placement-margin-only failure zone anywhere under the current config: every offset either succeeds cleanly (xy_err a few mm) or fails at SETTLE_APPROACH (grasp/reachability, before any grasp attempt). Substituted `x_plus_0.03` (a genuine, deterministic SETTLE_APPROACH failure, previously documented in Phase 4A/4B as IK-unreachable at 27.1mm residual) as the collection's labeled failure episode instead, and documented it honestly as a grasp/reachability failure rather than mislabeling it a placement failure to match the original plan. Both the stale measurement and the substitution rationale are recorded in the manifest's own `instruction_variants` object -- not silently swapped.
- Implemented the transition convention (`observation_t -> action_t -> physics_substeps -> observation_t+1`) at 10 Hz recording against 500 Hz physics (50 substeps/transition), with `observation_0` captured from a read-only zero-step probe (valid since this pipeline has no RNG anywhere) and every subsequent `action_{k-1}`/`observation_k` pair written by the same callback invocation, making `len(observations) == len(actions) + 1` a structural invariant rather than a runtime coincidence (asserted directly in the collector and re-checked by the validator/tests).
- Built the HDF5 schema with `policy_observations/` (RGB with no video-text overlay, joint positions/velocities, TCP pose, gripper state) strictly separated from a sibling `privileged/` group (cube/target pose+velocity, contact booleans/force, state-machine phase) -- never nested under the policy group. Installed `h5py` in `.venv` (authorized).
- Collected exactly 3 episodes via `tasks/g1_pick_place/record_demonstrations.py`: `nominal` (success, 132 transitions), `x_minus_0.03` (success, 132 transitions), `x_plus_0.03` (failure at SETTLE_APPROACH, 31 transitions, labeled `train_eligible=False`). `data/task1_prototype.hdf5`: 1,740,909 bytes -- small enough to commit directly. Exported 9 sample frames (`artifacts/phase5b_sample_frames/`) plus 3 observation-only visualization contact sheets; directly inspected several, confirming red cube / blue target both visible and the expected task progression.
- `tasks/g1_pick_place/validate_dataset.py`: all required checks pass, including independently RE-RUNNING the deterministic simulation per episode to recompute `task_pass` and compare against the stored `success` flag (not trusting the stored value) -- all three agreed. Exercised against 5 deliberately-tampered dataset copies in the test suite; all 5 caught.
- `tasks/g1_pick_place/replay_demonstration.py`: action replay (through the same `CubeInitGuard` pre-lock boundary, never overwritten again) measured against the original recording -- up to 4.9cm TCP / 15.2deg joint deviation on the two successful episodes, honestly attributed to the 10 Hz recorded action stream being a zero-order-hold downsample of the original per-physics-step waypoint ramp (`_drive_smooth`), not a replay bug; the reachability-failure episode (which never reaches a ramped segment) deviates by <1mm. Observation-only visualization replay mode (no physics stepped) also implemented and exercised. Manually verified replay correctly raises `ManifestMismatchError` against a tampered dataset copy.
- Added `tests/test_phase5b_dataset.py` (36 tests, generating a small temporary 2-episode HDF5 file rather than depending on the full dataset artifact being in git): manifest loading/hashing/mismatch-rejection, transition-convention invariants, HDF5 schema/dtype/content, validator tamper-detection (5 cases), replay determinism/manifest-enforcement, and a direct cross-check that Task 1's own success-threshold constants were not retuned.
- Full suite: 214 tests, 0 unexpected failures (178 pre-existing unchanged + 36 new). Confirmed no historical report/log/test/video from Phase 1 through 5A was altered; vendor pin/XML untouched; Task 1's own criteria/threshold constants confirmed byte-identical via a dedicated test. Stopped after exactly 3 episodes -- no scaled collection, no Task 2, no model training started.

## Phase 5C -- VLA action/replay fidelity fix (2026-09-03)

- User flagged Phase 5B's ~4.9cm nominal replay error as material (relative to the ~7cm cube) and not to be dismissed as merely an "expected 10Hz ZOH effect" without quantification. Audited the real timing (Section A): physics 500Hz, arm actuator target ramped and re-applied every physics step during LIFT/TRANSPORT_ABOVE_TARGET/LOWER_TO_TARGET (confirmed by reading `_drive_smooth`'s source directly -- the ramp loop is nested inside the per-waypoint loop), IK re-solved once per waypoint (20-30Hz depending on phase) but the ramp between solves updates at the full 500Hz. Classified every v1 action field's true semantics: `arm_joint_position_target`/`applied_ctrl` were an INSTANTANEOUS end-of-interval sample, never a value "held" for the interval -- Phase 5B's replay bug was treating that sample as a zero-order hold across the whole 100ms block, discarding the real intra-block ramp entirely.
- **Quantified the 4.9cm precisely**: replayed the same nominal episode's literal 500Hz `applied_ctrl` trace directly (no IK re-solve) and measured max TCP error of 3.65e-8m -- a ~1.3 million times reduction from Phase 5B's 4.87cm. This proves the entire error was the ZOH-discarded ramp, with no other meaningful contributor (not actuator lag, not IK nondeterminism).
- Built the two-rate schema: unchanged 10Hz `policy/` group (observations, high-level actions, RGB -- RGB never duplicated elsewhere) plus a new 500Hz `execution/` group (transition_index mapping each physics-step row to its parent 10Hz transition, literal applied ctrl, joint/TCP/cube state, fine-grained phase label). `execution_hz=500` chosen because that is the real controller set-point-change rate, verified in source, not picked arbitrarily.
- Implemented and clearly distinguished three replay modes in `tasks/g1_pick_place/replay_demonstration_v2.py`: (1) exact execution replay -- feeds the literal recorded `applied_ctrl` into `mj_step` every physics step; (2) policy-action replay -- decodes ONLY the 10Hz `high_level_actions` stream through a decoder reusing the same `solve_ik_waypoint`/`bounded_pd_step` primitives, re-ramping toward the recorded static per-phase goal every 100ms (the actual fix for Phase 5B's hold-constant bug); (3) observation-only visualization replay (zero physics stepped), same in spirit as Phase 5B's.
- **Exact execution replay result**: max TCP/joint/cube error 3.4e-8 to 7.7e-8 across all three episodes -- 4-5 orders of magnitude tighter than the 1e-4rad/1e-3m targets. Not literally bit-exact (floating-point accumulation over thousands of steps on a fresh MjData/model), reported as the measured/justified tolerance rather than an assumed zero.
- **Policy-action replay result, reported honestly rather than adjusted to pass**: the MAXIMUM TCP error during an episode does NOT meet the <=10mm target (measured ~97-98mm on the two successful episodes). Traced to the specific worst transitions (`LOWER_TO_TARGET_wp7` at 96.9mm, `TRANSPORT_ABOVE_TARGET_wp7` at 94.4mm) and diagnosed the mechanism: `cartesian_target` stores one static per-PHASE goal repeated across every 10Hz transition inside a multi-second ramped phase (e.g. all ~40 TRANSPORT_ABOVE_TARGET transitions share one value); a decoder with no waypoint-index information re-ramps toward that same distant goal every single 100ms interval instead of the true multi-waypoint path, producing a faster/more-direct path shape -- large MID-phase error, even though it still converges to the same final point (final TCP error 6.3mm on both successful episodes, within target). Documented as a genuine structural limitation of a static-per-phase-goal action representation, not a decoder bug; redesigning the stored action semantics is explicitly out of this phase's scope.
- Added `tests/test_phase5c_replay_fidelity.py` (17 tests): all 7 required transition-alignment properties (obs=actions+1, execution-to-transition mapping, execution timestamps within their transition interval, final-state agreement, no one-step action shift, consistent terminal-transition representation, reconstructed-action-matches-recorded-target) plus a deliberately shifted-action tamper test (`np.roll()`s a copied file's recorded `cartesian_target` by one transition; `validate_dataset_v2` correctly flags the mismatch).
- Collected `data/task1_prototype_v2.hdf5` (same 3 episode roles as Phase 5B: nominal/x_minus_0.03 success, x_plus_0.03 reachability failure), 4,995,223 bytes, committed directly. `x_plus_0.03`'s execution trace is 1550 rows spanning RESET through SETTLE_APPROACH only (no LIFT/TRANSPORT/LOWER content) because the trial genuinely never reaches CLOSE -- not a truncation bug, confirmed via the validator's row-count check.
- **Preserved Phase 5B completely**: `data/task1_prototype.hdf5`, `reports/phase5b-data-pipeline.md`, `data/schema.md`, `record_demonstrations.py`/`replay_demonstration.py`/`validate_dataset.py`, and commit `67ccf89` are byte-identical (verified via `git diff` against the pre-Phase-5C commit) -- all Phase 5C work is additive `_v2` files. `data/task1_canonical_config.json`'s scene/controller/gains/thresholds/camera content and hash are unchanged from Phase 5B.
- Fork stalled twice (600s watchdog) during this phase with zero visible cause both times; resumed each time by checking real `git status`/file contents from outside the process first (confirming the stalls were not caused by any hang in the code itself -- `record_demonstrations_v2.py` in fact ran cleanly end-to-end in ~20s when checked directly) and instructing more frequent checkpointing and isolated/backgrounded long-running steps (particularly the ~4-5 minute full-suite run) on resume.
- Full suite: 231 tests, 0 unexpected failures (214 pre-existing unchanged + 17 new). No push. Stopped after reporting both replay modes' errors -- no scaled collection, no Task 2.

## Phase 5D -- redesigned VLA policy-action representation (2026-09-03)

- User required resolving Phase 5C's remaining ~97-98mm max policy-action replay error before any scaling, with a specific technical direction: redesign the ACTION SEMANTICS (store the expert's next-interval command, not the phase's final goal), not just the decoder.
- Derived `policy/actions/{tcp_delta_position,tcp_delta_orientation,gripper_command}` from `execution/arm_joint_target` via forward kinematics (a new `forward_kinematics_tcp()` in `tasks/g1_pick_place/policy_action_codec.py`, using `mj_kinematics` on a scratch MjData with only the arm qpos slice overwritten -- never touching measured/tracked state or privileged cube/target state). Position delta: world frame (verified to coincide with the robot-base frame to ~0.19mm, the pelvis weld's softness independently measured in Phase 5A). Orientation delta: TCP-local body frame -- empirically verified (not assumed) that MuJoCo's `mju_subQuat(qa,qb)` returns the rotation vector `r` such that `qa = qb (x) axisAngle2Quat(r)` (right/body-frame composition, confirmed against a hand-built left-vs-right-composition discriminator) and that `mju_quatIntegrate` is its exact inverse.
- **Attempt 1** (single whole-interval delta, one IK solve + one linear joint-space ramp across 100ms): 23.6mm max TCP error (down from 96.9mm, still over the 10mm target). Traced the first divergence to transition 0 (RESET-to-PREGRASP, a 5.85cm commanded jump): `_drive_segment` phases issue a fixed target ONCE and hold it for many subsequent transitions, so the true trial had far more than 100ms to converge -- Attempt 1's decoder instead forces its OWN commanded reference to fully reach the same target within one interval, starving the position-servo of settle time the true trial actually had.
- **Attempt 2** (ramp-speed sweep from an immediate step through the full 50-step ramp): no single speed worked everywhere -- a fast ramp fixed the PREGRASP jump (4.7mm) but made a LATER, larger RETREAT-phase jump much worse (~49.5mm), and vice versa. Diagnosed as a genuine information gap (a 100ms-wide delta cannot describe a trajectory containing a large sub-100ms-scale reference change), not a tuning problem -- moved to Attempt 3 per the authorized budget.
- **Attempt 3 (shipped)**: fixed-size sub-action chunks, H=5 sub-deltas per 10Hz transition (50Hz sub-action rate, 10 physics steps each), each decoded with the same one-IK-solve-plus-one-ramp primitive applied per sub-chunk. Swept H in {1,2,5,10}: H=5 measured 8.09mm (nominal) / 5.99mm (x_minus_0.03), both under the 10mm target; H=2 still failed (43.1mm, confirming chunk granularity -- not just chunk presence -- matters); H=10 measured marginally better (7.36mm) but was not shipped since H=5 already met the target with margin.
- Exact execution replay confirmed unchanged from Phase 5C (~3.4-4.4e-8m across all three episodes) -- the `execution/` group and its replay path were not touched by this phase.
- `x_plus_0.03` (pre-grasp reachability failure) measured 12.77mm max policy-replay error -- the <=10mm bar applies only to successful episodes per this phase's authorization; this episode's actual requirement (identical failure stage/label) was met and reported separately, not folded into the successful-episode claim.
- Added `tests/test_phase5d_policy_actions.py` (25 tests): synthetic-delta recovery, rotation-composition-order/frame verification, decoder chunk-size consistency, causality (the auxiliary `next_arm_joint_target` field reconstructed purely from its own transition's `execution/arm_joint_target`, never privileged state), no off-by-one action shift (verified via independent forward kinematics against the reset boundary), no-phase-goal-repetition (a pass on the real dataset plus a tamper test injecting the old repeated-static-goal bug pattern and confirming the validator catches it), terminal/padding behavior, canonical-manifest-hash and new decoder-configuration-hash mismatch rejection (both in replay and the validator), and the shipped 10mm policy-replay regression on both successful episodes.
- **Preserved Phase 5B/5C completely**: `data/task1_prototype.hdf5`, `data/task1_prototype_v2.hdf5`, their reports/schemas, and commits `67ccf89`/`965b947` are byte-identical (verified via `git diff` against the pre-Phase-5D commit) -- all Phase 5D work is additive `_v3` files. `data/task1_canonical_config.json`'s scene/controller/gains/thresholds/camera content and hash are unchanged.
- Full suite: 256 tests, 0 unexpected failures (231 pre-existing unchanged + 25 new). No push. Stopped after reporting both policy-action and exact-execution replay errors -- no scaled collection began.

## Phase 5E -- scaled Task 1 demonstration collection (2026-09-03)

- User asked for a target-position distribution alongside cube position. Discovered mid-implementation that the blue target pad is fixed MJCF geometry (`tasks/g1_pick_place/gripper_scene.py`, `TARGET_POS`, no offset parameter) -- a first attempt to parameterize only the controller's internal target (`run_trial_pick_place(..., target_xy_offset=...)`) would have left the rendered pad in place while the true placement target silently moved, a physically dishonest RGB/ground-truth mismatch. Reverted that change completely (`git checkout -- tasks/g1_pick_place/run_pick_place.py`, confirmed empty `git diff`) rather than either faking the visual or modifying Task 1 geometry (both prohibited). Disclosed this conflict and the resulting decision (target fixed at (0,0) for all 32 episodes) in `data/task1_collection_spec.json` and the report BEFORE running the final collection, not after.
- Pilot (12 full trials + 2 IK-only reachability grids, `logs/phase5e_pilot_*.json`, kept separate from the final dataset): calibrated a continuous `cube_dx in [-0.035,-0.005]`, `cube_dy in [-0.01,0.035]` envelope -- IK-reachable (`diagnose_pick_place_reachability`, position-only) and physically confirmed at all 4 corners + center. Found `cube_dx=0.0` is IK-reachable only for `cube_dy>=0.0`, an asymmetric boundary not assumed in advance.
- Locked `data/task1_collection_spec.json` BEFORE the final run: 24 configs sampled continuously via `numpy.random.default_rng(seed)` (fully deterministic per integer seed), split 16/4/4 train/val/test by three disjoint `cube_dx` bands (genuine spatial held-out cells, verified non-overlapping both in the spec and in the actual sampled values); 8 fixed diagnostic probes (3 expected reachability rejections extending the known x_plus_0.03 direction, 2 reachability-boundary probes, 3 physical-outcome probes) with deliberately uncertain outcomes, not reused from the pilot's exact configs.
- Reused Phase 5D's v3 action schema/decoder (`policy_action_codec.py`, `record_demonstrations_v3._derive_policy_actions`) completely UNCHANGED -- confirmed via identical `decoder_configuration_hash`. New collector (`collect_dataset.py`) needed no per-phase Cartesian-goal bookkeeping at all, since v3 actions derive purely from `execution/arm_joint_target` via forward kinematics, independent of cube/target position.
- Reachability pre-filter applied before every physics trial: a config that fails costs 0 simulated seconds (verified in the collection summary), and is still recorded (not silently dropped) as `outcome=rejected_by_reachability`.
- **Actual yield differs honestly from the intended 24 success / 8 diagnostic split, reported as measured rather than forced**: all 24 success-envelope configs succeeded (100% hit rate -- exactly the intended 16/4/4 BC-training pool); of the 8 diagnostic probes, only 3 were rejected and 1 failed physically -- the other 4 (whose outcomes were genuinely uncertain by design) turned out to succeed. Final: 32 attempted, 28 successes, 4 diagnostic (1 failed_interaction + 3 rejected). No episode discarded or seed replaced to force a different count; all 8 diagnostics-split episodes keep `train_eligible=False` regardless of individual outcome, so the 24-episode BC pool is exactly as intended.
- **Honest replay-fidelity finding at scale**: exact execution replay 29/29 within 1mm (max 3.31e-5m). Policy-action replay (Phase 5D's H=5 decoder, unchanged) meets the <=10mm target on only 22/29 episodes (76%) -- 7 reach up to 22.3mm. Diagnosed (not fixed -- decoder held unchanged per authorization) as large single-transition reference jumps at some sampled cube positions exceeding what a fixed H=5/50Hz chunk can track; the dataset's own action-magnitude distribution shows sub-action deltas up to 149mm against a ~1mm typical mean. This shows Phase 5D's H=5 tuning, validated against only 3 configs, does not generalize uniformly across a continuous position envelope -- reported for a future phase to address if exact-per-episode policy-replay fidelity at scale becomes a requirement.
- `data/task1_demonstrations_v1.hdf5`: 29 episodes, 62,196,309 bytes -- left untracked (exceeds the 20MB commit-size threshold, unlike the small `task1_prototype*.hdf5` files); checksum/regeneration command recorded in `logs/phase5e_collection_summary.json` and the report instead.
- Installed `matplotlib` into `.venv` (new dependency, not used by any prior phase) to generate the 5 required Section G plots (`artifacts/phase5e_dataset_summary/`).
- Full suite: 288 tests, 0 unexpected failures (256 pre-existing unchanged + 32 new). No push. Stopped after dataset validation and the report -- no Task 2, no further scaling without new authorization.

## 2026-09-03 Phase 6 (final entrance-test submission package)

- Documentation/packaging phase only -- no code, dataset, or model changes. Read every prior phase report/log plus `HANDOFF.md`/`docs/work_log.md` in full to source every figure in the submission report and failure narrative from an actual prior measurement, not from memory or invention.
- Built `data/task1_demonstrations_v1_quality.json` programmatically from `logs/phase5e_validation.json` + `logs/phase5e_collection_summary.json` (never hand-transcribed) -- a 29-row per-episode table plus the four required Section-A answers: 18/24 BC-pool episodes pass the <=10mm policy-replay gate; the 7 dataset-wide offenders span train(5)/test(1)/diagnostics(1), zero in val; RETREAT-phase divergence never affects placement/success (RETREAT is strictly post-release in the state machine, and the offenders' final placement error, 1.5-4.2mm, matches the passing episodes' range, 1.1-4.1mm; zero of 29 episodes diverge before release); recommended default mask `task_execution_through_release` (24/24 BC pool). Does not alter the HDF5's own labels.
- **REPRODUCE.md verification caught two real risks, both corrected before proceeding**: (1) `git status` after test-running `run_pick_place`/`record_onboard_camera_episode`/`record_demonstrations_v3`/`validate_scaled_dataset` showed several already-committed historical files modified (`logs/phase4b_pick_place_trials.json`, `logs/phase5a_camera_smoke.json`, `logs/phase5d_v3_collection_summary.json`) -- each command was backed up first; `phase5a_camera_smoke.json`/`phase5d_v3_collection_summary.json`/all `artifacts/phase5e_dataset_summary/*` and `artifacts/phase5b_sample_frames/*` outputs reproduced byte-identical (confirmed via SHA-256 and `git diff`), but `phase4b_pick_place_trials.json`'s Stage-B success rate has legitimately changed (67%->100%) since Phase 4E's later gripper/trajectory fixes -- restored via `git checkout --` rather than left showing a diff, since this phase's job is to report, not to update, historical Phase 4B evidence. (2) `brew install pandoc` was started for the optional PDF, then aborted mid-install on discovering it pulls in a full GHC (Haskell) compiler build from source (30-60+ minutes, multi-GB) -- killed before completing, nothing left installed; likely also explains one anomalously slow (~3.5min vs ~26s) `record_demonstrations_v3` timing observed during the same window (resource contention, not a code issue, confirmed by a clean re-time afterward). No PDF was produced; documented honestly as "no reliable conversion available" rather than fabricated.
- `submission/entrance_test_report.md`: full 14-section report plus a chronologically-reordered failure narrative (the authorization's bullet order places the decorative-hand discovery before the slip-metric fix, but the real dated history in `HANDOFF.md` has the slip-metric audit/correction, Phase 4C, predating the decorative-hand discovery, Phase 4D -- corrected to true chronological order, all 13 required points covered with sourced figures).
- `submission/REPRODUCE.md`: 11 command groups, every one actually executed and timed in this environment this phase (not estimated) -- e.g. nominal Task 1 full sweep measured at 51-77s across two runs (real machine variance, not flaky), scaled collection cited from its own already-recorded Phase 5E log rather than re-run (deterministic, ~5.5min, no verification value added by re-running).
- `submission/DATASET_CARD.md`, `submission/results_summary.json` (every metric tagged measured/derived/human_reviewed/passed/failed/not_attempted).
- `submission/videos/`: `task1_third_person.mp4` = `artifacts/phase4f_task1_full.mp4` (the video Phase 4F's human acceptance decision actually reviewed -- re-checked against `HANDOFF.md` rather than assumed to be Phase 4E's, which was reviewed and *not* approved); `task1_onboard_rgb.mp4` = `artifacts/phase5a_head_camera.mp4`; `optional_debug_before_after.mp4` = new side-by-side composite (Phase 4D left / Phase 4E-corrected right, both trimmed to 4.30s/29.41fps) built with the existing `imageio_ffmpeg` binary, no new dependency. All three decode-verified (`ffmpeg -f null -`); `submission/video_manifest.json` records real ffprobe-derived metadata + SHA-256 for each, not fabricated values.
- Full suite re-run: 288 tests, 0 unexpected failures, 650.355s. Confirmed via `git diff` that this phase touches nothing under `tasks/g1_pick_place/` (read-only) and the canonical manifest hash is unchanged. Vendor pin unchanged; the pre-existing Go2w `terrain.STL` artifact and `logs/g1_mujoco_smoke.json` timestamp drift left untouched. No push.

## 2026-09-03 Task 2 (optional, time-boxed: language-conditioned two-object selection)

- Authorized as optional/time-boxed on a new branch, `task2-language-selection`, created from `35f15a5` (the accepted Task 1 submission commit). Does not touch Task 1's own dataset/reports/schemas/commits.
- Added a second cube ("cube2", green, identical size/mass/friction to the existing red cube) by re-parsing `write_grasp_scene_5a()`'s own output -- never editing that function or `write_grasp_scene_4b()`. Parametrized `run_pick_place.run_trial_pick_place` with four new OPTIONAL arguments (`cube_body_name`/`cube_geom_name`/`cube_joint_name`, defaulting to Task 1's exact literal names, and `distractor`, defaulting to `None`) so the same, otherwise-unmodified control logic can act on a caller-chosen body and track a second body's displacement as read-only telemetry -- confirmed byte-for-byte unaffected for every existing call site (Task 1's own nominal trial re-run: `task_pass=True`, `final_xy_target_error_m≈1.72mm`, matching every prior phase's documented figure).
- **Real empirical finding, not a guess**: an initial distractor-slot candidate 8cm away from the grasp slot (chosen because Task 1's finger-open width is only ~7.5cm) looked geometrically safe but measured 48.7mm of real distractor displacement in a full physics trial. Traced it with a `frame_callback` probe (printing distractor displacement per phase) to `RETREAT`'s pre-existing one-shot joint-space `_drive_segment` -- never Cartesian-smoothed in Task 1 because Task 1 never had a second object nearby for an uncontrolled joint-space sweep to hit. A second candidate, moved away from the target's pull direction, fixed the safety problem (5.0mm) but was barely visible to the onboard camera (9/19,200 green pixels, mostly occluded by the resting gripper). A three-way search (IK reachability + measured physical displacement in both grasp directions + camera pixel count, checked together rather than one at a time) found a slot meeting all three requirements at once: 0.0mm/1.7mm distractor displacement and 30/19,200 green pixels visible.
- `parse_selected_object()` is a deliberately trivial keyword lookup over exactly the two authorized instruction strings -- not a language model, not visual recognition; the physical task is driven by an explicit `selected_object_id` argument, never re-derived from text inside the control loop, matching the required reporting language verbatim in the report.
- Minimum evaluation: all 4 required configurations (red/green selected x nominal/swapped arrangement) x 3 deterministic trials each -- 12/12 pass. Wrong-object placement never occurs (0/12); distractor displacement 0.0-1.73mm in every trial; onboard camera confirmed to see both objects and the target at reset. One representative episode per successful configuration (4 total) logged; a full Phase-5E-scale dataset was explicitly not built (not required for this optional phase).
- Two videos recorded (`artifacts/task2_red_instruction.mp4`, `artifacts/task2_green_instruction.mp4`) via the existing, purely-observational `frame_callback` hook; both decode-verified frame-by-frame via `imageio` (389/389 frames each, matching the count recorded at capture time).
- 23 new tests (`tests/test_task2_language_selection.py`), including 2 explicit Task-1-non-regression tests. Full suite re-run: 288 pre-existing + 23 new = 311 tests, 0 unexpected failures. Committed on `task2-language-selection` only as `"feat: add language-conditioned two-object Task 2"` -- not merged to `main`, not pushed.
